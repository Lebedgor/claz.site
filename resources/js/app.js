import { Livewire, Alpine } from '../../vendor/livewire/livewire/dist/livewire.esm.js'

window.Alpine = Alpine

// Livewire starts itself on DOMContentLoaded when livewireScriptConfig is absent
// (public pages disable Livewire's injected script, see AppServiceProvider).
